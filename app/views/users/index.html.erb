<% provide(:title, 'All users') %>
<div class="container">

<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.avatar.url(:thumb) %>
      <%= link_to user.firstname, user %>
    </li>
  <% end %>
</ul>
</div>